# Roadmap — chỉ ghi nhận, KHÔNG tự ý triển khai

Mọi mục dưới đây cần được yêu cầu rõ ràng ở 1 sprint sau mới triển khai. Không tự ý code trước khi được giao.

> **SPRINT 7 COMPLETED (Requirement #1-#5 + Requirement #6 Sprint Review).** Requirement #1 (AI Observability Dashboard), #2 (AI Cost Tracking), #3 (AI Context Foundation), #4 (User-triggered Workflow Automation), #5 (AI Workflow Insights) đã hoàn tất và đã tái xác nhận (19/19 kịch bản kiểm thử PASS, Regression/Architecture/Security Verification PASS) ở Requirement #6 — xem `docs/SPRINT_7_FINAL_REPORT.md` cho báo cáo đầy đủ, `SPRINT_7_PLANNING.md` cho bối cảnh (mục tiêu Sprint 7: đưa PSH từ "AI Content Platform" sang "AI Automation Platform"). Cost Tracking (Requirement #2) là ƯỚC TÍNH — KHÔNG phải Billing/token thật. Context Foundation (Requirement #3) chỉ xây `ContextBuilder` dùng chung — Sprint 2-6 vẫn tự `loadContext()` riêng, CHƯA migrate. Workflow Automation (Requirement #4) CHỈ chạy khi Admin chủ động bấm "Chạy Workflow" — KHÔNG có Cron/Scheduler/Webhook/Auto Trigger/Background Automation nào; định nghĩa Workflow KHÔNG lưu vào Firebase. Workflow Insights (Requirement #5) chỉ quan sát (Observability), hoàn toàn CHỈ ĐỌC — Timeline mỗi giai đoạn con (Context/Provider/Draft) hiện GỘP CHUNG vì Queue không ghi tách riêng. Sprint 2, 3, 4, 5, 6 cũng đã COMPLETED. Cả 8/8 plugin viết từ Sprint 1 đều Production — không còn plugin nào ở trạng thái "coming_soon". Lưu ý còn treo: "AI Workflow Engine" tự động thật sự (Cron/Trigger nền, từng được gọi là "Requirement #2" ở nhiều Context Sprint 5) **vẫn chưa từng được triển khai** — Requirement #4 chỉ giải quyết phần "User-triggered", KHÔNG phải Automation nền thật sự; cần quyết định rõ ràng có tiếp tục làm phần tự động hay không (đây sẽ là thay đổi `AI_RULES.md`/Constitution, cần Decision Record riêng). Backup Google Drive không khả dụng trong môi trường hiện tại (Auto Mode Safety Classifier chặn hành động upload source code ra ngoài) — GitHub vẫn là nơi backup duy nhất. **SPRINT 8 Requirement #1 (Firebase Database Rules) đã hoàn tất** (`database.rules.json`, 15/15 kịch bản kiểm thử PASS qua mô phỏng — xem `CHANGELOG.md`) — **CHƯA deploy lên môi trường thật** (cần Firebase CLI, thao tác vận hành). **SPRINT 8 Requirement #2 (Media Library dùng chung cho CMS) đã hoàn tất** (`js/media-library.js`/`js/media-library-picker.js`, 9/9 kịch bản kiểm thử PASS — xem `CHANGELOG.md`) — **LƯU Ý: đây là thay đổi hướng so với đề xuất "Job Queue Concurrency Safety" ở mục "Sprint 8 Planning" bên dưới** — Chief Architect đã chọn Media Library làm Requirement #2 thay vì mục đã đề xuất. **SPRINT 8 Requirement #3 (Job Queue Concurrency Safety) đã hoàn tất** (khoá mềm `aiJobs/{jobId}/lock` qua Firebase `transaction()`, 5/5 kịch bản kiểm thử PASS mô phỏng 2 tab đồng thời — xem `CHANGELOG.md`) — giải quyết đúng Architecture Challenge #2 còn treo từ Requirement #2.

> **SPRINT 8 COMPLETED (Requirement #1-#3 + Requirement #4 Sprint Review).** Requirement #1 (Firebase Database Rules), #2 (Media Library), #3 (Job Queue Concurrency Safety) đã hoàn tất và đã tái xác nhận bằng mã nguồn thật (41/41 kịch bản kiểm thử PASS, Regression/Architecture/Security Verification PASS) ở Requirement #4 — xem `docs/SPRINT_8_FINAL_REPORT.md` cho báo cáo đầy đủ. Database Rules **chưa deploy** lên Firebase thật (thao tác vận hành, không phải code). Media Library xây hoàn toàn trên Storage đã có, không đổi Database Structure; `DataProvider`/`ContextBuilder` CHƯA đọc từ Media Library mới (vẫn chỉ suy ra ảnh từ `product.images`). Job Queue Concurrency Safety chỉ vá đúng race condition đã biết (TTL 5 phút là cấu hình, không phải đảm bảo tuyệt đối) — không mở rộng sang race condition tương tự ở các CMS module khác (`makeListDB.update()`). 1 phát hiện bảo mật cũ ("Xung đột #1" — nghi `admin-users.js` gãy sau khi deploy Rules) đã được **đính chính** ở Requirement #4 sau khi đọc lại đúng mã nguồn (không phải lỗi thật — xem mục "Firebase Database Rules" bên dưới); "Xung đột #2" (cảnh báo sai ở trang đăng nhập) vẫn còn, chưa sửa. Sprint 2-7 cũng đã COMPLETED, không bị ảnh hưởng. **Lưu ý quan trọng**: file `SPRINT_9_PLANNING.md` xuất hiện (untracked) trong working tree ở thời điểm đóng Sprint 8 — sản phẩm của 1 phiên làm việc TRƯỚC đó đã nhầm branch (`claude/cms-image-experience-2-u89lud`, dự án Next.js/MySQL khác, không liên quan PSH Platform thật). Nội dung đó **đã được thay thế hoàn toàn** bằng Sprint 9 Planning thật của nhánh này (rà soát `CHANGELOG.md`/`PROJECT_ARCHITECTURE.md`/`ROADMAP.md`/`AI_RULES.md`/`README.md` — phát hiện chính: `README.md` có hướng dẫn Firebase Rules sai/kém an toàn hơn `database.rules.json` đã review). CHƯA bắt đầu Sprint 9, chờ Chief Architect lập Sprint 9 Planning.

> **SPRINT 9 Requirement #1 (Documentation Integrity Restoration) đã hoàn tất** — sửa `README.md` (hướng dẫn Firebase Rules sai/kém an toàn hơn `database.rules.json`; claim "mọi provider là stub" đã lỗi thời; bảng CMS Admin thiếu 7 trang AI Sprint 4-7; tham chiếu `docs/SPRINT_2_PROGRESS.md` lỗi thời) và `AI_RULES.md` (gộp 2 cặp mục bị đánh trùng số "§7"/"§8", cập nhật "Giới hạn phạm vi hiện tại" cho khớp thực tế — 8/8 plugin Production, Media Library đã có) — xem `CHANGELOG.md`. Không đổi bất kỳ quy tắc/ràng buộc nào trong `AI_RULES.md`, không sửa mã nguồn/Database/Firebase Rules/AI Framework/Workflow/Plugin. Phát hiện phụ: con số `escapeHtml()` trùng lặp thật là 26 file (không phải 10) — đã cập nhật ở mục "Sprint 8 Planning" bên dưới.

> **SPRINT 9 Requirement #2 (Firebase Database Rules Production Alignment) đã hoàn tất** — đối chiếu `database.rules.json`/`firebase.json`/`README.md`, xác nhận không còn mâu thuẫn nội bộ (`firebase.json` trỏ đúng file, `README.md` đã hết cấu hình sai từ Requirement #1). **Kiểm thử nâng cấp**: chạy 19/19 kịch bản trên **Firebase Realtime Database Emulator thật** (không phải mô phỏng bằng tay như Sprint 8) — bằng chứng mạnh hơn hẳn rằng Rules trong repo tự nhất quán, đúng thiết kế. Tạo mới `docs/FIREBASE_RULES_DEPLOYMENT.md` — quy trình đầy đủ để người vận hành đối chiếu Rules đang chạy thật, deploy (`firebase deploy --only database`), và xác minh sau deploy. **KHÔNG tuyên bố Production đã khớp Rules** — môi trường này không có quyền truy cập Firebase project thật (`firebase projects:list` báo lỗi xác thực, đã xác nhận không suy đoán) — `database.rules.json` vẫn chưa từng deploy, kế thừa nguyên trạng từ Sprint 8 Requirement #1. CHƯA làm Requirement #3, chờ Chief Architect giao.

> **SPRINT 9 Requirement #3 (Firebase Storage Security Rules) đã hoàn tất** — tạo mới `storage.rules` (KHÔNG tồn tại trong repo trước Requirement này), tách quyền `get`/`list`/`write` theo Firebase Storage Rules v2: `get` công khai (giữ nguyên hotlink ảnh trang khách), `list` chỉ người đã đăng nhập (**THẮT CHẶT** — đóng khoảng hở `listAll()` công khai mà ruleset thủ công cũ có thể đang ngầm cho phép), `write` (create+update+delete) người đã đăng nhập, không phân biệt vai trò (đúng hành vi Media Library thật). Wiring `firebase.json` (`"storage": {"rules": "storage.rules"}`), sửa `README.md` trỏ đúng file thật. **Kiểm thử trên Firebase Storage Emulator thật** (cùng bộ `firebase-tools`/`@firebase/rules-unit-testing` đã cài ở Requirement #2) — 9/9 kịch bản PASS. Tạo mới `docs/FIREBASE_STORAGE_RULES_DEPLOYMENT.md` cùng cấu trúc với tài liệu Database Rules. **Giới hạn kỹ thuật xác nhận được**: KHÔNG thể siết `write` theo đúng vai trò Admin/Editor như Database Rules — Storage Rules không có cách nào cross-reference node `roles/{uid}` của Realtime Database, cần Firebase Auth Custom Claims (ngoài phạm vi, xem mục "Firebase Storage Security Rules" bên dưới). **KHÔNG tuyên bố Production đã khớp Rules** — `storage.rules` chưa từng deploy, cùng giới hạn môi trường đã ghi nhận ở Requirement #2. Không đổi Media Library (xác nhận qua `git diff`). CHƯA làm Requirement #4, chờ Chief Architect giao.

## Sprint 8 Planning (đề xuất — chờ phê duyệt, CHƯA triển khai)

**Mục tiêu đề xuất**: chuyển trọng tâm từ "xây tính năng" (Sprint 2-7) sang "củng cố nền tảng cho Production thật" — đóng 2 khoảng hở lớn nhất phát hiện ở Architecture Challenge (Firebase Rules chưa xác minh được, Queue có rủi ro race condition khi nhiều tab/admin cùng hoạt động) trước khi xây thêm tính năng mới.

**Architecture Challenge — phân loại** (rà soát toàn bộ AI Framework sau Sprint 7, không khen, chỉ tìm vấn đề):

| # | Vấn đề | Loại | Phân loại |
|---|---|---|---|
| 1 | ~~`database.rules.json` không version-control trong repo~~ | Architecture Risk / Security Gap | ✅ **Đã làm** — Requirement #1 (`database.rules.json`, xem `CHANGELOG.md`) — **còn thiếu bước deploy thật lên Firebase** |
| 2 | ~~`AIJobQueue` dùng biến `processing` cục bộ theo từng tab/phiên trình duyệt...~~ | Architecture Risk / Concurrency | ✅ **Đã làm** — Requirement #3 (khoá mềm `aiJobs/{jobId}/lock` qua Firebase `transaction()`, xem `CHANGELOG.md`) — xem mục "Job Queue Concurrency Safety" bên dưới cho giới hạn còn lại |
| 3 | `escapeHtml()` bị lặp lại nguyên văn ở **10 file** `js/admin-ai*.js` (tính cả các file từ Sprint 2-7); `rangeStartMs()` lặp lại ở **3 file** `js/ai/{usage-stats,cost-tracking,workflow-insights}.js`. **Cập nhật ở Sprint 9 Requirement #1**: grep lại toàn bộ `js/` (không chỉ `admin-ai*.js`) cho thấy con số thật là **26 file** — thêm cả file storefront/CMS ngoài AI (`site-chrome.js`, `category.js`, `blog-list.js`, `blog-post.js`, `videos-list.js`, `home.js`, `admin-categories.js`, `admin-products.js`, `admin-blog.js`, `admin-banners.js`, `admin-sliders.js`, `admin-videos.js`, `admin-menu.js`, `admin-footer.js`, `admin-users.js`, `media-library-picker.js`). `rangeStartMs()` = 3 file, con số cũ chính xác, không đổi. | Technical Debt / Duplication | **B — đưa ROADMAP**, không khẩn cấp (rủi ro thấp, chỉ ảnh hưởng maintainability) |
| 4 | `ContextBuilder` (Sprint 7 #3) chưa được Plugin nào dùng — tồn tại như hạ tầng chờ, không tạo giá trị thật cho tới khi có Plugin áp dụng | Over-Engineering (có kiểm soát) | **B — đưa ROADMAP**, chỉ đáng làm khi có Plugin mới thật sự cần |
| 5 | Chỉ 1/4 Provider (OpenAI) có tích hợp thật; Claude/Gemini/DeepSeek vẫn là stub vĩnh viễn — "Provider Independence" đúng về kiến trúc nhưng chưa từng được chứng minh với Provider thứ 2 thật | Production Gap | **B — đưa ROADMAP**, chờ quyết định kinh doanh có cần đa dạng Provider hay không |
| 6 | Cloud Function `openaiProxy` chưa deploy lên môi trường thật — AI Framework chưa từng xử lý 1 lượt Generate thật nào trong Production | Production Gap | **C — không cần sửa code**, đây là thao tác vận hành (`firebase deploy`), không phải vấn đề kiến trúc |
| 7 | `AITaskRouter` vẫn rule-based, danh sách route dạng mảng cứng — mở rộng thêm Plugin "chủ đề tự do" (Topic-only Routing) có rủi ro từ khóa trùng lặp giữa các route khi số Plugin tăng | Technical Debt (tiềm ẩn) | **C — không cần sửa ngay**, chưa phát sinh vấn đề thật vì Router hiện chỉ có 3 route |

**Đề xuất Requirement Sprint 8 (thứ tự ưu tiên)**:

1. **Firebase Database Rules — Version Control + Verification** (ưu tiên cao nhất, giải quyết Architecture Challenge #1). Đưa `database.rules.json` vào repo, đối chiếu đúng với `AI_PERMISSIONS`/`ROLE_PERMISSIONS` đã có trong `permission-service.js`, xác nhận Firebase Rules THẬT SỰ chặn được request trái quyền (không chỉ dựa vào kiểm tra phía client). Không đổi `permission-service.js`/`AI_RULES.md` (chỉ thêm Rules tương ứng).
2. **Job Queue Concurrency Safety** (giải quyết Architecture Challenge #2) — cần Decision Record riêng trước khi bắt đầu vì đây là thay đổi trực tiếp vào `job-queue.js` (Queue), file đã bị khoá "không sửa" xuyên suốt Sprint 2-7. Mục tiêu: phát hiện/ngăn 2 phiên xử lý trùng cùng lúc — có thể chỉ cần "khoá mềm" qua Firebase (vd 1 field `processingBy`/`lockedAt` trong `aiJobs` hoặc 1 node khoá riêng) thay vì đổi kiến trúc Queue V1 → V2 hoàn toàn.
3. **Kiểm tra toàn diện + Đóng Sprint** (Sprint Review bắt buộc, theo đúng quy trình đã áp dụng từ Sprint 3).

**Không đưa vào Sprint 8** (đã ghi nhận, chờ quyết định riêng nếu muốn làm sau): hợp nhất `escapeHtml`/`rangeStartMs` (mục 3), migrate Plugin sang `ContextBuilder` (mục 4), tích hợp thật Claude/Gemini/DeepSeek (mục 5), deploy Cloud Function (thao tác vận hành, không phải Requirement code), mở rộng Topic-only Routing cho AI Task Router, Automation nền thật sự (Cron/Trigger — vẫn cần sửa Constitution trước).

> **CẬP NHẬT sau khi giao Requirement #2 thật sự**: Chief Architect đã chọn **Media Library dùng chung cho CMS** làm Requirement #2 của Sprint 8 (thay vì "Job Queue Concurrency Safety" đã đề xuất ở mục 2 phía trên). "Job Queue Concurrency Safety" (Architecture Challenge #2) được giao lại làm **Requirement #3** ngay sau đó — xem mục "Job Queue Concurrency Safety" ngay dưới cho kết quả thật sự, và mục "Media Library" cho kết quả Requirement #2.

## Job Queue Concurrency Safety (Sprint 8, Requirement #3)

- ✅ ~~`AIJobQueue` không đồng bộ được giữa nhiều tab/phiên Admin~~ — **đã làm**: khoá mềm `aiJobs/{jobId}/lock` (`lockedBy`, `lockedAt`), giành bằng Firebase `transaction()` (nguyên tử thật, không phải đọc-rồi-ghi), tự hết hạn sau 5 phút (`LOCK_TTL_MS`) nếu không được làm mới. 5/5 kịch bản kiểm thử PASS (mô phỏng 2 tab độc lập bằng Node `vm`, dùng chung 1 kho Firebase giả lập) — xem `CHANGELOG.md`.
- **TTL 5 phút là cấu hình, không phải đảm bảo tuyệt đối** — nếu 1 lượt xử lý item AI thật sự kéo dài hơn 5 phút mà không có tiến triển để làm mới khoá (`refreshJobLock()` chỉ chạy sau khi 1 item xử lý XONG), một tab khác về lý thuyết vẫn có thể giành lại khoá giữa chừng và xử lý trùng đúng item đó. Rủi ro thấp với thời gian xử lý AI Generate thực tế hiện tại (giây tới vài chục giây/lượt), nhưng nếu tương lai có Plugin xử lý cực chậm (vd file lớn, batch nhiều bước con trong 1 item), nên cân nhắc giảm TTL hoặc thêm heartbeat làm mới khoá GIỮA chừng xử lý 1 item (không chỉ sau khi xong) — chưa triển khai, ngoài phạm vi Requirement #3 (chỉ xử lý đúng race condition đã nêu, không mở rộng thêm).
- **Không khắc phục race tổng quát của `makeListDB.update()`** (`js/cms-db.js`) — factory này vẫn dùng kiểu đọc-rồi-ghi (không atomic) cho MỌI CMS module khác (Category/Banner/Blog/Video/Slider...), không riêng gì Queue. Requirement #3 chỉ thêm 1 lớp khoá atomic RIÊNG cho việc "giành quyền xử lý 1 Job" trong `job-queue.js` — không refactor `makeListDB`/sửa cách các module CMS khác ghi dữ liệu (đúng phạm vi "chỉ sửa Queue", không mở rộng Sprint). Nếu các module CMS khác cũng cần chống race (vd 2 Admin cùng sửa 1 sản phẩm), đó là 1 Requirement riêng.
- **Chưa kiểm thử trên Firebase Realtime Database thật** — môi trường hiện tại không có Firebase CLI/project thật để deploy/kiểm thử trực tiếp (giống giới hạn đã ghi nhận ở Requirement #1/#2); kiểm thử lần này mô phỏng đúng ngữ nghĩa `transaction()` đã công bố của Firebase (tuần tự hoá theo từng path, atomic) bằng Node `vm`, chính xác hơn cách mô phỏng DOM/Chrome đã dùng ở Requirement #1/#2 vì môi trường phiên này có sẵn Node.js — nhưng vẫn không thay thế được việc xác minh trên Firebase thật trước khi Production.
- **`processing` (biến cục bộ theo tab, có từ Sprint 2 Requirement #6) vẫn giữ nguyên, không xoá** — vẫn hữu ích để chặn nhanh việc 1 tab tự gọi `resume()` chồng lên chính nó (vd double-click/nhiều timer); khoá Firebase mới là lớp bảo vệ CHÉO giữa các tab, bổ sung chứ không thay thế lớp này.

## Media Library (Sprint 8, Requirement #2)

- ✅ ~~Media Library CMS module~~ — **đã làm** (`js/media-library.js`, `js/media-library-picker.js`) — kho ảnh dùng chung cho Product/Blog/Banner/Slider/Category, xây hoàn toàn trên Firebase Storage đã có, không thêm Database node nào. Mục cũ "Media Library CMS module" (ghi từ Sprint 2/3, "ảnh hiện quản lý rời rạc theo từng field... cần có trước khi Slider Generator/Banner Generator thật sự đọc Media Library đúng nghĩa") **coi như đã giải quyết phần "kho ảnh trung tâm cho CMS"** — phần "AI Plugin đọc Media Library" (DataProvider/ContextBuilder) vẫn CHƯA làm, xem mục ngay dưới.
- **AI Framework (`DataProvider.getMedia()`, `ContextBuilder`) CHƯA tích hợp với Media Library mới** — theo đúng Architectural Constraint "Không thay đổi Data Provider" của Requirement #2, `js/ai/data-provider.js` vẫn suy ra ảnh từ `product.images` như cũ, KHÔNG đọc từ Media Library chung. Nếu muốn AI Plugin (Slider Generator/Banner Generator) đọc được toàn bộ Media Library thay vì chỉ ảnh của 1 Product, cần 1 Requirement riêng sửa `data-provider.js` (Architectural Constraint của Requirement đó sẽ phải cho phép, hiện đang bị khoá).
- ✅ ~~Storage Security Rules (`storage.rules`) chưa version-control trong repo~~ — **đã làm** ở Sprint 9 Requirement #3 (`storage.rules`, tách `get`/`list`/`write`, 9/9 kịch bản kiểm thử PASS trên Firebase Storage Emulator thật) — xem mục "Firebase Storage Security Rules" bên dưới.
- **`StorageUpload.attachUploadInput()` (Sprint 1) hiện không còn nơi nào gọi tới** — Media Library đã thay thế toàn bộ 5 chỗ dùng cũ (Product/Blog/Banner/Slider/Category). Không xóa file `js/storage-upload.js` (không sửa/xóa gì ở đây, `MediaLibrary.upload()` vẫn dùng `uploadImage()` của file này) — chỉ ghi nhận `attachUploadInput()` hiện là dead code, có thể dọn ở 1 Requirement dọn dẹp sau này nếu muốn.
- **Media Library chưa hỗ trợ Video/PDF** — kiến trúc đã sẵn sàng mở rộng (chỉ cần đổi điều kiện lọc `contentType` trong `MediaLibrary.list()`), nhưng OUT OF SCOPE của Requirement #2 (đã ghi rõ trong Requirement). Để dành cho 1 Requirement riêng nếu cần.
- **Không có Duplicate Detection** — 2 file ảnh giống hệt nhau upload 2 lần sẽ tạo 2 bản ghi Storage riêng (đúng Out of Scope của Requirement #2, không tự triển khai).

## Firebase Database Rules (Sprint 8 Requirement #1, Sprint 9 Requirement #2) — phát hiện phát sinh khi triển khai

- ✅ ~~Triển khai `database.rules.json`~~ — **đã làm** (13 node CMS/AI + `roles`, wiring qua `firebase.json`), kiểm thử 15/15 kịch bản PASS qua mô phỏng biểu thức Rules thật (Chrome, không có Firebase CLI trong môi trường hiện tại).
- ✅ **Sprint 9 Requirement #2 — kiểm thử lại bằng Firebase Realtime Database Emulator THẬT** (không phải mô phỏng bằng tay): cài tạm `firebase-tools`/`@firebase/rules-unit-testing`, nạp đúng `database.rules.json` đã commit vào Emulator, chạy 19 kịch bản qua `authenticatedContext()`/`unauthenticatedContext()` chính thức — **19/19 PASS**. Bằng chứng mạnh hơn hẳn Sprint 8 rằng Rules trong repo tự nhất quán — xem `docs/FIREBASE_RULES_DEPLOYMENT.md` mục 5 cho danh sách kịch bản đầy đủ.
- **CHƯA deploy lên môi trường thật** — cần `firebase deploy --only database` (Firebase CLI + đăng nhập), vẫn không thực hiện được trong môi trường phát triển hiện tại (đã xác nhận ở cả Sprint 8 và Sprint 9: `firebase projects:list` báo lỗi xác thực, không có quyền truy cập project `pshop-music` thật). **Đã chuẩn bị đầy đủ để deploy an toàn** ở `docs/FIREBASE_RULES_DEPLOYMENT.md` (Sprint 9 Requirement #2) — quy trình đối chiếu Rules đang chạy thật trước khi deploy, lệnh deploy, checklist xác minh sau deploy, rollback. **Không tuyên bố Production đã khớp Rules** — chỉ người vận hành có quyền truy cập Firebase Console/CLI thật mới xác nhận được.
- **Cập nhật ở Requirement #4 (Sprint Review)**: "xung đột #1" bên dưới đã được rà soát lại và nhiều khả năng KHÔNG phải lỗi thật (xem chi tiết) — không còn là điều kiện bắt buộc phải xử lý trước khi deploy, nhưng vẫn khuyến nghị xác nhận thủ công 1 lần trên Firebase thật sau khi deploy (đã đưa vào checklist của `docs/FIREBASE_RULES_DEPLOYMENT.md`).
- **Xung đột #1 — ĐÃ RÀ SOÁT LẠI Ở REQUIREMENT #4 (Sprint Review), KHÔNG PHẢI LỖI THẬT**: ghi nhận ban đầu ở Requirement #1 cho rằng `js/admin-users.js` sẽ gãy vì `createUserWithEmailAndPassword()` tự động chuyển phiên đăng nhập hiện tại sang tài khoản mới tạo (đúng là hành vi chung của Firebase Auth, đã xác nhận qua kịch bản #13 mô phỏng Rules). Tuy nhiên, đọc lại đúng mã nguồn thật `js/admin-users.js` (dòng 64–68) cho thấy hàm `createUser()` đã dùng **1 Firebase App phụ** (`firebase.initializeApp(firebaseConfig, 'Secondary-'+Date.now())`) để gọi `createUserWithEmailAndPassword()` — đây là pattern đã có sẵn từ Sprint 1 (đúng như docstring đầu file mô tả: "via a secondary Firebase app instance so creating a user doesn't sign the current admin out"), CHỦ ĐÍCH để không ảnh hưởng phiên đăng nhập chính. Việc ghi `roles/{uid mới}` (dòng 66) gọi qua `firebase.database()` (App CHÍNH, không phải App phụ) — tại thời điểm ghi, `auth.uid` của App chính vẫn là Admin đang thao tác, không đổi. Đối chiếu với kịch bản Rules #13 ("Admin cấp quyền cho uid mới thành công" — `auth.uid` là Admin, ghi `roles/{uid khác}`) — **đúng luồng này, không phải luồng "tự ghi bằng chính uid mới" mà kịch bản #12 mô tả** — Rules cho phép. **Kết luận: tính năng "Thêm tài khoản mới" nhiều khả năng KHÔNG bị gãy sau khi deploy Rules**, khác với ghi nhận ban đầu. Chưa kiểm thử được trên Firebase Auth + Database thật cùng lúc (chỉ xác minh được qua đọc mã nguồn + mô phỏng Rules riêng lẻ) — khuyến nghị vẫn nên xác minh thủ công 1 lần trên môi trường thật sau khi deploy, nhưng KHÔNG còn xem đây là việc bắt buộc phải sửa code trước khi deploy.
- **Xung đột #2 (thấp, chỉ ảnh hưởng thông báo, không ảnh hưởng chức năng)**: `admin/login.html` sẽ hiện cảnh báo "Chưa kết nối được hệ thống phân quyền..." ở MỌI lần vào trang đăng nhập sau khi có Admin đầu tiên (vì đọc `roles` công khai bị Rules chặn đúng thiết kế, không phải lỗi) — thông báo hiện tại không phân biệt được "Rules chưa cấu hình" với "Rules đã đúng, chỉ không cho đọc công khai nữa". Nên sửa `js/admin-login.js` để không hiện cảnh báo này khi lỗi là do quyền bị từ chối ở trạng thái đã có Admin (không phải lỗi thật).
- **Giới hạn kiến trúc phát hiện (không phải lỗi, có sẵn từ Sprint 1)**: `siteContent` gộp chung tính năng chỉ-Admin (menu/footer/settings) và tính năng Admin+Editor (Slider Manager) trong 1 node Firebase, ghi theo kiểu ghi đè toàn bộ object (`SiteContentDB.save()`) — Rules chỉ có thể cho phép "Admin HOẶC Editor ghi cả node", không tách được chính xác theo từng tính năng con. Muốn Rules chặn đúng theo từng tính năng cần tách `siteContent` thành các node riêng (Database Structure change, cần Decision Record) hoặc `.validate` phức tạp so khớp field con (rủi ro cao, chưa kiểm thử được trên Firebase thật).

## Firebase Storage Security Rules (Sprint 9 Requirement #3) — phát hiện phát sinh khi triển khai

- ✅ ~~Triển khai `storage.rules`~~ — **đã làm** — tách `get`/`list`/`write` theo Firebase Storage Rules v2 (không tồn tại trong repo trước Requirement này), wiring qua `firebase.json`, kiểm thử 9/9 kịch bản PASS trên **Firebase Storage Emulator thật** (không phải mô phỏng bằng tay) — xem `docs/FIREBASE_STORAGE_RULES_DEPLOYMENT.md` mục 5 cho danh sách kịch bản đầy đủ.
- **CHƯA deploy lên môi trường thật** — cần `firebase deploy --only storage` (Firebase CLI + đăng nhập), không thực hiện được trong môi trường phát triển hiện tại (kế thừa cùng giới hạn đã xác nhận ở Sprint 9 Requirement #2 cho Database Rules: `firebase projects:list` báo lỗi xác thực). **Đã chuẩn bị đầy đủ để deploy an toàn** ở `docs/FIREBASE_STORAGE_RULES_DEPLOYMENT.md` — quy trình đối chiếu Rules đang chạy thật trước khi deploy (đặc biệt vì Rules thủ công cũ có thể đang gộp `read` kiểu v1, khiến `list` công khai đang mở), lệnh deploy, checklist xác minh sau deploy, rollback. **Không tuyên bố Production đã khớp Rules** — chỉ người vận hành có quyền truy cập Firebase Console/CLI thật mới xác nhận được.
- **Giới hạn kỹ thuật đã xác nhận (không phải lỗi, là giới hạn thật của Firebase Storage Rules)**: không thể siết `write` theo đúng vai trò `roles/{uid}` (Admin/Editor) như `database.rules.json` đã làm cho Realtime Database — Storage Security Rules KHÔNG có cách nào cross-reference node Realtime Database (chỉ hỗ trợ `firestore.get()`/`firestore.exists()` cho Cloud Firestore, dự án dùng Realtime Database, không dùng Firestore). Muốn Storage Rules biết "uid này còn active trong `roles` hay đã bị thu hồi" cần Firebase Auth **Custom Claims** (đặt qua Admin SDK/Cloud Function khi tạo/thu hồi tài khoản ở `admin/users.html`) — đây là thay đổi Business Logic + Cloud Function mới, cần Decision Record + Chief Architect phê duyệt riêng nếu triển khai sau, ngoài phạm vi Requirement #3.
- **Không có thông tin về Rules thủ công đang chạy thật trên Firebase Console** — không suy đoán đang là dạng v1 (`allow read, write: if request.auth != null`) hay đã có ai chỉnh tay khác đi; người vận hành phải tự đối chiếu trước khi deploy (xem `docs/FIREBASE_STORAGE_RULES_DEPLOYMENT.md` mục 1).

## AI Workflow Insights (Sprint 7, Requirement #5)

- ✅ ~~Triển khai AI Workflow Insights~~ — **đã làm** (`js/ai/workflow-insights.js`, `admin/ai/workflow-insights.html`, `js/admin-ai-workflow-insights.js`) — Timeline từng AI Request (Context → Queue → Provider → Draft → Human Review → Trạng thái cuối), hoàn toàn chỉ đọc từ `aiJobs`/`aiLogs`/`aiDrafts` đã có.
- **Timeline hiện chỉ tách được 2 mốc lớn: "Queue chờ" và "Xử lý" (gộp Context+Provider+Draft)** — Queue (`job-queue.js`) không ghi timestamp riêng cho từng giai đoạn con (lúc bắt đầu đọc Context, lúc gọi xong Provider, lúc tạo xong Draft), nên Insights không thể tách chi tiết hơn mà không đổi Queue. Muốn có Timeline chi tiết từng giai đoạn con (đúng nghĩa Distributed Tracing) cần Queue ghi thêm timestamp — LÀ 1 thay đổi Queue/Database Structure, cần Decision Record + Chief Architect phê duyệt riêng nếu triển khai sau (đúng NFR "có khả năng mở rộng thành Distributed Tracing trong tương lai" của Requirement #5).
- **Khớp Log với từng item trong Job theo thứ tự xử lý, không theo khoá riêng** — `aiLogs` không lưu `itemIndex`, nên với Job có nhiều item (batch), Insights khớp Log thứ i với item thứ i theo đúng thứ tự tuần tự Queue đã xử lý (`processSequentially()`). Đúng với cách Queue xử lý thật (tuần tự, không song song) nhưng vẫn là suy luận theo thứ tự, không phải khoá liên kết trực tiếp — nếu cần khoá chính xác tuyệt đối, cần thêm field `itemIndex` vào `aiLogs` (thay đổi Database Structure, cần Decision Record).
- **Sprint 7 đã đủ 5 Requirement phát triển tính năng đã giao** — Workflow Insights là Requirement cuối cùng của Sprint 7 theo Context đã nhận. Bất kỳ ý tưởng mới nào ngoài phạm vi Requirement #5 (nếu phát sinh khi triển khai) đã được ghi ở đây thay vì tự triển khai.

## User-triggered Workflow Automation (Sprint 7, Requirement #4)

- ✅ ~~Triển khai Workflow Automation (thủ công, chạy tuần tự nhiều Plugin)~~ — **đã làm** (`js/ai/workflow-engine.js`, `admin/ai/workflow.html`, `js/admin-ai-workflow.js`) — Admin ghép nhiều Step (mỗi Step = 1 Plugin), bấm "Chạy Workflow" để chạy tuần tự qua đúng Permission Service/Plugin Manager/Queue hiện có, dừng ngay khi 1 Step lỗi, không tự Publish.
- **Định nghĩa Workflow KHÔNG được lưu lại** — danh sách Step chỉ tồn tại trong bộ nhớ trình duyệt ở phiên đang mở, mất khi tải lại trang. Cố tình KHÔNG lưu vào Firebase (đúng Architectural Constraint "Không đổi Database Structure" của Requirement #4) — muốn lưu Workflow để chạy lại nhiều lần (vd đặt tên, chọn lại từ danh sách) cần 1 Collection mới (`aiWorkflows` hoặc tương tự), LÀ 1 thay đổi Database Structure, cần Decision Record + Chief Architect phê duyệt riêng.
- **Automation nền thật sự (Cron/Trigger tự động/Webhook) vẫn ngoài phạm vi** — Requirement #4 CHỈ giải quyết "User-triggered" (Admin tự bấm Run). Muốn Workflow tự chạy theo lịch/theo sự kiện cần sửa `AI_RULES.md` mục 3 ("Chỉ chạy khi có hành động rõ ràng của người dùng") — đây là thay đổi Constitution, phải có Decision Record + Chief Architect phê duyệt riêng trước khi làm (đúng NFR "có khả năng mở rộng sang Automation thật trong tương lai SAU KHI Constitution được sửa").
- **Mỗi Step chỉ chạy đúng 1 item, chưa hỗ trợ batch** — khác Dashboard chính (`admin/ai/index.html`) cho phép nhiều dòng = nhiều lần chạy cho 1 Plugin. Workflow hiện là chuỗi tuần tự "1 Plugin - 1 lần" mỗi Step, đúng ngữ nghĩa "Step 1 → Step 2 → ... → Step N" của Requirement. Mở rộng batch cho từng Step (nếu cần) để lại cho Requirement sau.
- **Chưa truyền dữ liệu output của Step trước làm input cho Step sau** — mỗi Step hiện độc lập, Admin phải tự điền input cho từng Step (vd không tự động lấy `productId` vừa tạo Draft ở Step 1 làm input cho Step 2). Đây là hướng mở rộng tự nhiên (data-passing giữa các Step) nhưng ngoài phạm vi "chạy tuần tự nhiều Plugin" đã giao ở Requirement #4 — ghi nhận nếu cần ở sprint sau.

## AI Context Foundation (Sprint 7, Requirement #3)

- ✅ ~~Triển khai Context Builder dùng chung~~ — **đã làm** (`js/ai/context-builder.js`, `admin/ai/context-builder.html`, `js/admin-ai-context-builder.js`) — đóng gói Context Package từ `DataProvider` (product/media/post/settings), có `toPromptText()` chứng minh tương thích ghép vào `prompt` của AI Provider. Hoàn toàn chỉ đọc, không Memory dài hạn, không RAG.
- **Sprint 2-6 CHƯA migrate sang dùng chung `ContextBuilder`** — mỗi module trong `js/ai/modules/*.js` vẫn tự viết `loadContext()` riêng (shape khác nhau: `{product}`, `{post}`, `{product,media}`, `{}`...), có phần trùng lặp logic với `ContextBuilder`. Cố tình KHÔNG refactor ở Requirement #3 (đúng Architectural Constraint "Không Refactor Sprint 2-6") — nếu muốn hợp nhất, cần 1 Requirement riêng (đổi `loadContext()` của từng module là thay đổi Sprint 2-6, phải được giao rõ ràng).
- **Context Builder hiện chỉ hỗ trợ `productId`/`postId`** (đúng 2 loại entity Data Provider có id để tra cứu) + `settings` làm nền tảng chung — chưa hỗ trợ `categoryId`/danh sách brand/nhiều entity cùng lúc. Mở rộng thêm nguồn Context (nếu cần) để lại cho Requirement sau, tránh mở rộng phạm vi ngoài "Context Foundation" đã giao.
- **AI Memory dài hạn** (lưu lại Context giữa các lượt, dùng cho hội thoại nhiều bước) là bước mở rộng tự nhiên tiếp theo của Context Builder (đúng NFR "có khả năng mở rộng thành AI Memory") — CHƯA triển khai, ngoài phạm vi Requirement #3 (Out of Scope rõ ràng). Nếu triển khai sau, cần thêm Collection Firebase mới để lưu lịch sử Context → LÀ 1 thay đổi Database Structure, phải có Decision Record + Chief Architect phê duyệt riêng trước khi làm.
- **RAG (tìm kiếm ngữ nghĩa/vector)** cũng ngoài phạm vi Requirement #3 — `ContextBuilder` hiện chỉ tra cứu trực tiếp theo id, chưa có khả năng tìm dữ liệu liên quan theo ngữ nghĩa. Ghi nhận nếu cần ở sprint sau.

## AI Observability Dashboard (Sprint 7, Requirement #1)

- ✅ ~~Triển khai AI Observability Dashboard~~ — **đã làm** (`admin/ai/observability.html`, `js/ai/observability.js`, `js/admin-ai-observability.js`) — gộp Health Check/Provider/Queue/Plugin/Usage/Job/Draft vào 1 màn hình, hoàn toàn chỉ đọc.
- **Auto-refresh/cảnh báo ngưỡng thời gian thực cho Observability Dashboard** — hiện chỉ tải lại khi bấm "Làm mới" hoặc đổi khoảng thời gian Usage; chưa có polling định kỳ hay cảnh báo tự động khi tỷ lệ lỗi vượt ngưỡng. Phát sinh khi triển khai Sprint 7 Requirement #1 — NFR của Requirement này chỉ yêu cầu "có khả năng mở rộng" (đã đáp ứng qua tách `compute()`/render), chưa yêu cầu tự làm tính năng cảnh báo thật.
- **Provider Status hiện chỉ đọc cấu hình (`aiProviderConfig`), không tự gọi `health()` cho từng Provider chưa active** — chỉ Provider đang active mới được kiểm tra sống qua Health Check; 3 Provider còn lại (nếu enable) chỉ hiển thị trạng thái cấu hình. Quyết định có chủ đích ở Sprint 7 Requirement #1 để tránh gọi mạng thừa cho Provider không dùng tới (đúng NFR "Không phụ thuộc AI Provider") — nếu cần kiểm tra sống cho mọi Provider, cần 1 Requirement riêng.

## AI Assistant

- ✅ ~~Kích hoạt các plugin còn lại đang "Coming Soon"~~ — **tất cả 8 plugin viết từ Sprint 1 đã được kích hoạt sang Production, không còn plugin nào "coming_soon".** ✅ ~~FAQ Generator~~ — Sprint 5 Requirement #3. ✅ ~~Blog Writer~~ — Sprint 6 Requirement #1 (qua Plugin Manager Dashboard, chưa tích hợp AI Assistant). ✅ ~~Facebook Post Generator~~ — Sprint 6 Requirement #2 (`targetCollection: null`, Draft chỉ xem/copy, không ghi CMS). ✅ ~~Banner Generator~~ — Sprint 6 Requirement #3 (`targetCollection: 'banners'`, publish thật qua `BannerDB.add()`). ✅ ~~Image Prompt Generator~~ — **đã kích hoạt** ở Sprint 6 Requirement #4 (`targetCollection: null`, cùng dạng Facebook Post Generator — chỉ sinh văn bản Prompt, không tự gọi API tạo ảnh, xem mục ngay dưới).
- **Topic-only Routing cho AI Assistant sẽ được xem xét ở một Requirement riêng trong tương lai.** Phát sinh khi kích hoạt FAQ Generator ở Sprint 5 Requirement #3: `AITaskRouter.matchTarget()` hiện bắt buộc khớp 1 Product/Blog Post thật, nên FAQ Generator/Blog Writer/Facebook Post Generator/Banner Generator/Image Prompt Generator (đều dạng "chủ đề tự do", không nhắm thực thể có sẵn) không thể định tuyến qua AI Assistant — hiện chỉ dùng được qua Plugin Manager Dashboard. Chief Architect đã quyết định KHÔNG mở rộng Router ở Sprint 5 (Decision Record — Option B, xem `CHANGELOG.md` Sprint 5 Requirement #3) — việc mở rộng Router (nếu có) để lại cho 1 Requirement riêng sau này. Xác nhận lại: Blog Writer (Sprint 6 Requirement #1), Facebook Post Generator (Sprint 6 Requirement #2), Banner Generator (Sprint 6 Requirement #3) và Image Prompt Generator (Sprint 6 Requirement #4) cũng chưa tích hợp AI Assistant, cùng lý do.
- **Thêm field "Model" riêng cho Product** — hiện thông tin model đang gộp chung trong field `specs` (text tự do), không tách riêng. Phát sinh khi rà soát Requirement #2 (Sprint 3) — cần đổi Database Structure (Product schema) nên KHÔNG tự làm, chỉ ghi nhận.
- **Product Description Generator: dùng `description` hiện có làm ngữ cảnh** — hiện plugin luôn viết mô tả mới hoàn toàn từ `name/brand/specs/category`, chưa tận dụng `description` đã có (nếu có) để viết lại/mở rộng thay vì thay thế. Phát sinh khi rà soát Requirement #2 (Sprint 3) — ngoài phạm vi Requirement #2 (chỉ "kích hoạt sang Production", không đổi Prompt/Interface), để sprint sau nếu được giao.
- **SEO AI Plugin cho Product** — hiện SEO Generator (`js/ai/modules/seo-generator.js`) chỉ nhắm vào Blog Post, chưa hỗ trợ Product, vì Product chưa có trang chi tiết riêng để gắn Meta/OG/Schema (xem mục "SEO cho trang sản phẩm riêng" bên dưới — đây là cùng 1 giới hạn, ghi nhận lại khi rà soát Requirement #3, Sprint 3).
- **Tách riêng field "Focus Keyword"** cho SEO Generator — hiện đang gộp chung trong mảng `keywords` (nhiều từ khóa), chưa có 1 từ khóa chính riêng biệt. Phát sinh khi rà soát Requirement #3 (Sprint 3).
- **AI gợi ý URL Slug** cho SEO Generator — chưa làm vì đổi slug của bài đã publish có rủi ro phá link cũ (SEO/backlink); cần thiết kế riêng (vd chỉ áp dụng cho bài chưa publish) trước khi triển khai. Phát sinh khi rà soát Requirement #3 (Sprint 3).
- **Prompt Optimization** cho các plugin đã Production (Product Description Generator, SEO Generator) — prompt hiện là bản cố định từ Sprint 2, chưa qua vòng tối ưu/A-B test thực tế với OpenAI. Phát sinh khi rà soát Requirement #3 (Sprint 3).
- **Prompt Versioning** — chưa có cơ chế lưu lịch sử thay đổi prompt theo thời gian cho từng plugin. Phát sinh khi rà soát Requirement #3 (Sprint 3).
- ✅ ~~Cost Tracking ước tính theo Provider/Plugin~~ — **đã làm** ở Sprint 7 Requirement #2 (`admin/ai/cost-tracking.html`, `js/ai/cost-tracking.js`): ước tính chi phí = số lượt Generate thành công × đơn giá tham khảo tĩnh (chỉ có cho OpenAI), KHÔNG cần đổi Database Structure. **Vẫn còn treo** (chưa làm, cần Decision Record riêng nếu triển khai): Cost Tracking dựa trên **token/chi phí thật** — cần thêm field mới vào `aiLogs` (vd `tokensUsed`, đọc từ field `usage` có sẵn trong response OpenAI qua Cloud Function Proxy) — đây LÀ 1 thay đổi Database Structure, phải có Decision Record + Chief Architect phê duyệt riêng trước khi triển khai. Xem thêm mục "Theo dõi chi phí/quota" đã ghi ở dưới (cùng ý, gộp lại).
- **Slider Generator: field `ctaText` hiện không có tác dụng** — Draft content có field này nhưng `js/home.js` không đọc (nút CTA dùng text cố định trong HTML, chỉ đổi `link`). Cần quyết định: làm cho field này có tác dụng thật (đổi text nút CTA theo từng slide) hoặc bỏ hẳn. Phát sinh khi rà soát Requirement #4 (Sprint 3), không sửa vì là Refactor ngoài phạm vi "chỉ kích hoạt sang Production".
- **Slider Generator: để AI tự gợi ý nội dung nút CTA** — hiện nội dung nút chỉ là lựa chọn cố định từ dropdown `ctaStyle` (Admin chọn tay), không phải do AI sinh ra. Phát sinh khi rà soát Requirement #4 (Sprint 3).
- **Job Queue V2**: chuyển xử lý `aiJobs` sang Firebase Cloud Functions (trigger theo dữ liệu) — xử lý tuần tự thật sự, không phụ thuộc trình duyệt Admin còn mở hay không.
- ✅ ~~Bảo mật API key provider~~ — **đã làm cho OpenAI** ở Sprint 3 Requirement #1 qua Cloud Function Proxy (`functions/openaiProxy`, key trong Secret Manager, không lưu Firebase/không lộ client). Xem `ARCHITECTURE_REVIEW_SPRINT3.md`.
- **Áp dụng lại mẫu Cloud Function Proxy cho Claude/Gemini/DeepSeek** khi các provider này được tích hợp thật ở sprint sau — dùng đúng khuôn mẫu `functions/index.js` đã có, không thiết kế lại (xem `ARCHITECTURE_REVIEW_SPRINT3.md`, phân loại C).
- **Rate limiting cho Cloud Function Proxy** — hiện chỉ xác thực (Firebase Auth + `roles`), chưa giới hạn tần suất gọi/quota theo user. Cân nhắc khi số lượng tài khoản CMS tăng (xem `ARCHITECTURE_REVIEW_SPRINT3.md`, phân loại B).
- **Đổi region Cloud Function** sang gần Việt Nam hơn (hiện `us-central1`) để giảm độ trễ, nếu cần tối ưu (phân loại C).
- **Job Queue V3**: dedicated queue service (vd Cloud Tasks) khi quy mô lớn hơn.
- **Media Library CMS module**: kho ảnh trung tâm (duyệt/tìm/tái sử dụng ảnh đã upload) — hiện ảnh chỉ quản lý rời rạc theo từng field. Cần có trước khi Slider Generator/Banner Generator thật sự "đọc Media Library" đúng nghĩa.
- **SEO cho trang sản phẩm riêng**: SEO Generator mở rộng sang Product — cần có trang chi tiết/URL riêng cho từng sản phẩm trước (hiện Product chỉ hiển thị dạng lưới + modal trên `category.html`).
- **AI Image Generation**: tích hợp API tạo ảnh thật (dùng `imagePrompt` đã có từ Slider Generator/Image Prompt Generator làm input) — chưa làm ở sprint nào tới hiện tại. Image Prompt Generator (Sprint 6, Requirement #4) chỉ sinh VĂN BẢN Prompt để người dùng tự dán sang công cụ khác — xác nhận qua kiểm thử là không có bất kỳ lời gọi API tạo ảnh nào trong Draft Workflow hiện tại.
- **AI Video**: chưa có kế hoạch cụ thể.
- Sửa nội dung Draft bằng rich-text (Quill) thay vì xem JSON thô trong `admin/ai/drafts.html`.
- Theo dõi chi phí/quota sử dụng theo từng provider.

- **Version-control Firebase Realtime Database Rules** (`database.rules.json`) trong repo thay vì chỉ quản lý trên Firebase Console — giúp review/rollback rules dễ hơn, đặc biệt sau khi Cloud Function `openaiProxy` bắt đầu đọc node `roles`. Phát sinh khi rà soát Requirement #5 (Sprint 3).
- **Test tự động (CI)** cho `job-queue.js`/`permission-service.js`/3 plugin Production/`task-router.js` — mới kiểm thử thủ công qua mô phỏng Node `vm` ở từng Requirement, chưa phải bộ test chạy tự động trong CI.
- **Lưu nguyên văn "User Request"** — hiện Conversation History (Sprint 4, Requirement #4) chỉ hiển thị mô tả suy ra (outcomeLabel + tên đối tượng), không phải câu tự do người dùng đã gõ, vì hệ thống hiện không lưu chuỗi đó ở đâu. Nếu cần xem lại NGUYÊN VĂN câu gõ, cần thêm 1 field mới vào `aiJobs` (vd `userRequestText`) — đây LÀ 1 thay đổi Database Structure, cần Decision Record riêng và Chief Architect phê duyệt trước, không tự triển khai.
- **Phân trang thật (Firebase query pagination)** cho Conversation History/Job Queue/Nhật ký khi số lượng tăng lớn — hiện cả 3 trang đều tải toàn bộ node rồi cắt bớt phía client (`.slice()`), không dùng `limitToLast`/`orderByChild` của Firebase. Phát sinh khi rà soát Sprint 4 Requirement #4.
- **Phân biệt Job tạo từ AI Assistant vs Plugin Manager cũ** — hiện Conversation History hiển thị chung tất cả `aiJobs` vì không có field phân biệt nguồn gốc; nếu cần tách riêng, đó cũng là 1 thay đổi Database Structure (thêm field nguồn gốc Job), cần Decision Record.
- **Mở rộng Constitution (`AI_RULES.md`) để `AI Task Router` được ghi Log** khi không xác định được Plugin/đối tượng (`plugin_not_found`/`target_not_found`/`target_ambiguous`) — hiện các trường hợp này CHỈ hiển thị ở UI (`js/admin-ai-assistant.js`), không ghi vào `aiLogs`, vì Constitution hiện chỉ cho phép `AIJobQueue`/`PermissionService` ghi Log (xem `PROJECT_ARCHITECTURE.md` mục "AI Assistant — Experience Layer"). Cần Chief Architect quyết định có nên sửa Constitution hay không — không tự ý mở rộng ở Sprint 4 Requirement #1.
- **Intent Analysis dùng AI thật thay vì rule-based** — `AI Task Router` hiện chỉ khớp từ khóa/tên thực thể cố định (không gọi OpenAI, không dùng AI Provider nào), theo đúng ràng buộc "không gọi OpenAI trực tiếp/không thêm AI Provider mới" của Sprint 4 Requirement #1. Nếu muốn Router hiểu ý định linh hoạt hơn (câu phức tạp, nhiều cách diễn đạt), cần thiết kế riêng cách Router gọi AI mà vẫn không phá ranh giới kiến trúc hiện tại (vd: có nên xem đây là 1 "Plugin nội bộ" gọi qua đúng Provider Manager, hay cần cơ chế mới) — để ngỏ, không quyết định trước.
- ✅ ~~Giao diện chọn thủ công khi đối tượng bị "ambiguous"~~ — **đã làm** ở Sprint 4 Requirement #3 (bảng chọn Tên/Danh mục/ID/Ngày tạo, không cần gõ lại yêu cầu).
- ✅ ~~Gộp trạng thái Job/Draft ngay trong AI Assistant~~ — **đã làm** ở Sprint 4 Requirement #2 (theo dõi tiến trình + Draft Preview + Publish/Reject tại chỗ qua `publishDraftById()`/`rejectDraftById()`).

## CMS / Hạ tầng chung

- WordPress theme (`wordpress-theme/`) hiện lạc hậu so với kiến trúc CMS chính — đồng bộ lại nếu được yêu cầu dùng song song.
- Xem thêm các mục Roadmap kỹ thuật khác (nếu phát sinh) trong `README.md`.
